# Test environment for Symfony (demo app). Used when APP_ENV=test.
# Copy to .env.test.local if you need local overrides (do not commit).

APP_ENV=test
APP_DEBUG=0
APP_SECRET=test_secret_change_me
DATABASE_URL=mysql://demo_user:password@mysql:3306/dashboard_menu_demo_test?serverVersion=8.0&charset=utf8mb4
